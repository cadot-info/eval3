{% extends 'base.html.twig' %}

{% block title %}Crypto{% endblock %}

{% block body %}
    <h1>Crypto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crypto.id }}</td>
            </tr>
            <tr>
                <th>Symbol</th>
                <td>{{ crypto.symbol }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ crypto.quantite }}</td>
            </tr>
            <tr>
                <th>Prix_achat</th>
                <td>{{ crypto.prixAchat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crypto_index') }}">back to list</a>

    <a href="{{ path('crypto_edit', {'id': crypto.id}) }}">edit</a>

    {{ include('crypto/_delete_form.html.twig') }}
{% endblock %}
